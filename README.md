# QEMU/KVM Virtualization Setup for CachyOS/Arch Linux

**One‑command script to install a full virtualization stack with performance optimizations.**

---

## Quick Install

```bash
# Clone the repo
git clone https://github.com/your-org/Virt-Setup.git
cd Virt-Setup

# Run the installer (requires sudo)
bash setup_virt.sh
